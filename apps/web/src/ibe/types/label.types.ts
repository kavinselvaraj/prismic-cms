export type IbeLabels = {
  flight_search: {
    airport: {
      label: string;
      name: string;
    };
    ptc: string;
  };
  flight_select: {
    from_date: string;
    to_date: string;
  };
};
